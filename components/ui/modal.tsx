'use client';import { cva, type VariantProps } from 'class-variance-authority';import { X } from 'lucide-react';import { ReactNode } from 'react';import { cn } from '@/lib/utils';import { Button } from './button';
const modalVariants=cva('fixed inset-0 z-50 grid place-items-center bg-black/70 p-6',{variants:{variant:{default:'backdrop-blur-sm'}}});export function Modal({open,onClose,children,variant}:{open:boolean;onClose:()=>void;children:ReactNode}&VariantProps<typeof modalVariants>){if(!open)return null;return <div className={cn(modalVariants({variant}))} role="dialog" aria-modal><div className="glass max-w-lg rounded-3xl p-6"><Button variant="ghost" className="float-right" onClick={onClose} aria-label="Close"><X size={18}/></Button>{children}</div></div>}
